# Battleship

## Running

Maven wrappers are provided to run the project without an IDE.

If using a unix-like shell, use `./mvnw` to run maven commands. Window shells, such as PowerShell or Command Prompt,
should use `./mvnw.bat`.

